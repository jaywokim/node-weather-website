<!DOCTYPE html>

<html>
    <head>
        <title>404</title>
        <link rel="icon" href="/img/weather.png">
        <link rel="stylesheet" href="/css/styles.css">
    </head>

    <body>
        <div class="main-content">
            {{>header}}
            <p>{{message}}</p>
        </div>
        {{!-- <h1>{{title}}</h1> --}}
        {{!-- {{>header}}
        <p>{{message}}</p> --}}
        {{!-- <p> Created by {{name}}</p> --}}
        {{>footer}}
    </body>

</html>